# Host / conda: producer + unit tests only.
# PyFlink lives in pyflink/requirements.txt and is installed via pyflink/Dockerfile.
python-dotenv>=1.0.0
requests==2.32.5
kafka-python==2.3.1
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pytest>=7.4.0
